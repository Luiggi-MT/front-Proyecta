npx expo export -p web
Genera una carpeta dist 
Se puede instalar un servidor http simple npm install -g http-server
y provarlo desde el directorio dist con 
cd dist
http-server
Provaremos a hacerlo desde nginx